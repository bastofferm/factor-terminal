# Python
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
*.egg-info/

# Node / Next.js
node_modules/
.next/
out/
npm-debug.log*

# Local configuration. .env is byte-identical to .env.example today, but it is
# where real credentials land the moment anyone fills one in — the FRED and
# DeepSeek keys are read from the user environment precisely so they never have
# to sit in a file, and this keeps it that way.
.env
.env.local
.env.*.local

# The concept note this repository implements. Excluded from a public repository
# because it is the one artefact here not written as part of this project; add it
# back by deleting this line if it is yours to publish.
Return_Based_Multi_Asset_Faktormodell.pdf

# Editors and OS
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db
desktop.ini

# Runtime output
*.log
scratch/
