[pytest]
testpaths = backend/tests
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    integration: needs a populated factors database (skipped when unavailable)
filterwarnings =
    ignore::DeprecationWarning
